apiVersion: v1
kind: Service
metadata:
  name: {{ include "tushare.fullname" . }}
  labels:
    {{- include "tushare.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tushare.selectorLabels" . | nindent 4 }}
